<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MyApp.Desktop.Pages.ServiceBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="RootGrid" Padding="28" RowSpacing="20">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Padding" Value="16" />
                        <Setter Target="RootGrid.RowSpacing" Value="14" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock x:Name="TitleText" Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock x:Name="SubtitleText" Opacity="0.72" TextWrapping="Wrap" />
        </StackPanel>

        <InfoBar
            x:Name="OperationInfoBar"
            Grid.Row="1"
            IsClosable="True"
            IsOpen="False" />

        <GridView
            x:Name="ServicesGrid"
            Grid.Row="2"
            ItemsSource="{x:Bind Services}"
            IsItemClickEnabled="False"
            IsMultiSelectCheckBoxEnabled="False"
            SelectionMode="None"
            SizeChanged="ServicesGrid_SizeChanged"
            VerticalAlignment="Stretch"
            VerticalContentAlignment="Top"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollBarVisibility="Auto">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid
                        ItemWidth="364"
                        MaximumRowsOrColumns="8"
                        Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0,0,14,14" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="MinHeight" Value="236" />
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border
                        MinHeight="236"
                        Padding="18"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <Grid RowSpacing="14">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="3">
                                    <TextBlock
                                        FontSize="18"
                                        FontWeight="SemiBold"
                                        Text="{Binding Item.Name}"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock
                                        Opacity="0.68"
                                        Text="{Binding Item.Publisher.Name}"
                                        TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <Border
                                    Grid.Column="1"
                                    Padding="9,4"
                                    Background="{ThemeResource SystemFillColorNeutralBackgroundBrush}"
                                    CornerRadius="8">
                                    <TextBlock FontWeight="SemiBold" Text="{Binding StateText}" />
                                </Border>
                            </Grid>

                            <StackPanel Grid.Row="1" Spacing="12">
                                <TextBlock
                                    MaxHeight="60"
                                    Opacity="0.82"
                                    Text="{Binding Item.Description}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="Wrap" />
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <TextBlock Opacity="0.72" Text="{Binding VersionText}" />
                                    <TextBlock
                                        MaxWidth="170"
                                        Opacity="0.72"
                                        Text="{Binding Item.Category}"
                                        TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </StackPanel>

                            <Grid Grid.Row="2" ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button
                                    Click="DetailsButton_Click"
                                    IsEnabled="{Binding IsDetailsEnabled}"
                                    Tag="{Binding Item.Id}">
                                    <TextBlock Text="{Binding DetailsText}" />
                                </Button>
                                <Button
                                    Grid.Column="1"
                                    MinWidth="132"
                                    Click="ActionButton_Click"
                                    IsEnabled="{Binding IsActionEnabled}"
                                    Tag="{Binding Item.Id}">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <ProgressRing
                                            Width="16"
                                            Height="16"
                                            IsActive="{Binding IsBusy}"
                                            Visibility="{Binding ProgressVisibility}" />
                                        <SymbolIcon
                                            Symbol="{Binding ActionIcon}"
                                            Visibility="{Binding IconVisibility}" />
                                        <TextBlock Text="{Binding ActionText}" />
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
